# TODO: confirm these match the values shown in SonarQube Cloud after the
# project is imported (Administration > General Settings). The values below
# follow SonarQube Cloud's default auto-generated keys for a GitHub import.
sonar.organization=biota-geom
sonar.projectKey=biota-geom_frontend-biota-geom

sonar.sources=src
sonar.tests=src/tests
sonar.exclusions=src/**/*.d.ts,src/main.tsx
sonar.javascript.lcov.reportPaths=coverage/lcov.info
sonar.typescript.lcov.reportPaths=coverage/lcov.info
